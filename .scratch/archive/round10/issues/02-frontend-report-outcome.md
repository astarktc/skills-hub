# 02: Wave A frontend — report → outcome is one pure fold (#4)

Status: done — 0f1bc91

**Lane:** frontend (Astra medium)  **Blocked by:** none. Base: `main` @ `7a7c519` (v1.2.9).
**Origin:** round-9 panel candidate #4 (`.scratch/round9/panel/architecture-review.html`; Opus 2, Fable 5, Astra 3 in the seat reports); operator rulings `.scratch/round10/decisions.md` Q6, Q13, Q19. Read those, then `AGENTS.md` (Frontend presentation logic / hook-level tests bullets), `CONTEXT.md` (Notification, Sync target, Update, Refresh), `~/.pi/agent/skills/codebase-design/SKILL.md`.
**Files:** new `src/lib/reportOutcome.ts` + `reportOutcome.test.ts`; `src/hooks/useSkillLibrary.ts` (+ `.test.ts`), `useAddSkillFlow.ts` (+ test), `useCandidatePick.ts` (+ test), `useSyncOrchestration.ts` (+ test); `src/i18n/resources.ts` only if a key moves (no new copy). Uses today's DTOs from `src/bindings/index.ts` as-is.
**Do NOT touch:** anything under `src-tauri/`, `src/bindings/`, `App.tsx`, components, `useStatusReporter.ts` (delivery stays there), `skillPresentation.ts` beyond an import. A parallel backend lane is changing the `setSkillInvocationOverride` return shape to `{ entry, propagation }`; leave `useSkillLibrary.ts:88-94` (the `setInvocationOverride` callback) **untouched** so the merge is clean — its report will be folded in a wave-B follow-up.

## F1 — the fold
Nine hand-rolled report interpretations live as `useCallback`s: `useSkillLibrary.ts` `skillFailureEntries` (:169), `targetFailureEntries` (:215), `skippedEntries` (:247), `editConflictEntries` (:262), the `handleRefresh` success/failed/skipped/conflict toast precedence (:276-330), `removalFailureEntries` (:332), `removalToast` (:349), `settleSingleReport` + `runSingleRefresh` (:555-600), the local Re-point path (:659-703, reloads only on success and ignores conflicts — a fork of the rule); plus `useAddSkillFlow.ts` (:331, :388), `useCandidatePick.ts` (:196-222) and `useSyncOrchestration.ts`'s `syncFailureEntries`. `managedSkillsRef` (:76-79) exists only because `skillFailureEntries` captured a list that moved under an in-flight notification (`70651c8`).
→ One pure module `src/lib/reportOutcome.ts`: for each report shape the app has (`RefreshReportDto` batch and batch-of-one, removal report, global sync batch report, import report, add/install result — enumerate what actually exists in bindings), a function `(report, ctx) → Outcome` where `ctx` carries only what the fold needs (`t`, tool-label lookup, a `canRepoint(skillId)` predicate or the ids it needs) and `Outcome = { toast: {kind, message} | null, errors: ActionErrorEntry[], warnings: ActionErrorEntry[], completion: { reload: boolean, closeModal: boolean, conflict: boolean } }`. Notification actions carry a **skill id**; the hook resolves the id against current data at click time (this deletes `managedSkillsRef`). One precedence rule (conflict › failed › skipped › success) shared by batch and batch-of-one; the Re-point divergence closes by construction — decide and document the unified rule (recommended: Re-point behaves exactly like Update).
Hooks: invoke → fold → execute the completion (reporter calls for toast/entries, `loadManagedSkills` if `reload`, close the modal if `closeModal`). No report field is read in a hook outside the fold.

## F2 — tests (Q19)
`reportOutcome.test.ts`: table tests over report fixtures per shape and precedence; the click-time id resolution is the hook's job, so the fold's action is asserted as an id. In `useSkillLibrary.test.ts` (1174 lines) delete the cases that only exercised the closures' text/ordering; keep or add **one** hook test per action (Update, Restore, both Re-points, Refresh-all, unsync, delete, sync-to-all, toggle) proving it calls the fold's outcome into the reporter and executes `completion` (reload called / not; modal closed / not). Same treatment for the other three hook tests. Net vitest count is allowed to go down; state the before/after in Comments.

## Gate
Worktree `/tmp/skills-hub-r10a-frontend`, branch `r10a/frontend` off `main` (`git worktree add /tmp/skills-hub-r10a-frontend -b r10a/frontend main && cd there && npm ci`). `npm run version:check && npm run check` green (`cargo test --all` will be untouched but run it). Conventional commits (`refactor(ui): report outcome fold`, `test(ui): …`). No push/merge/rebase, no `npm run tauri:dev`, `.scratch/` is gitignored — never `git add -f`. pi-lens commit guard block → `git diff` per item to `~/Projects/skills-hub/.scratch/round10/<item>.patch`, report, do not bypass. Final message `## Comments`: the `Outcome` type and fold signatures you chose, the unified precedence/completion rule, what was deleted from the hook tests, deviations, gate counts, worktree path + branch HEAD.

## Comments

- 2026-09-15 — Status reconciliation: ready-for-agent → done — 0f1bc91. Evidence: git log v1.2.9..v1.2.10 finds 0f1bc91, followed by 300f89b/c8bb3d4 for skips and single-action toast; src/lib/reportOutcome.ts:55,76 owns the fold and skill-ID actions. Wave A shipped v1.2.10; v1.2.11 includes it.
