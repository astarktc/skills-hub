# Round-7 review — Skill Edit V1 (one child per model; both axes in one brief)

Repo: `~/Projects/skills-hub-r7b` (a git worktree on branch `r7/skill-edit-v1` — READ ONLY; do not edit, commit, run mutating npm/cargo commands, or launch the app; `cargo test`/`npm test`/`npm run build` are fine to confirm something). Do not search outside this directory (cloud-synced folders). Do not run `npm run tauri:dev` (it mutates the operator's real skill library). Do not `git add` anything; `.scratch/` is gitignored on purpose. AGENTS.md Workflow step 1 does not apply to a review — start immediately. Write your report file **incrementally** (append each section as you finish it) so a dropped session leaves partial work.

## What to review
- Fixed point: `ac8b514` (v1.2.6). HEAD = `a261e89` (one commit). Diff: `git diff ac8b514...HEAD`; 26 files, ~1100 lines.
- Spec: `.scratch/round7/spec.md` (decisions E3–E11 are this ticket; E1/E2 shipped in v1.2.6). Ticket: `.scratch/round7/issues/02-skill-edit-v1.md` — the brief the implementer followed; treat it as the spec's operational form. Implementer's claims (verify them): committed one commit; deviations declared: (1) Save is permitted on an unchanged-but-conflicted override so the operator can re-base; (2) base metadata also records original frontmatter-block presence and duplicate key lines for exact restoration. Files outside the ticket's expected list: `core/skill_edits.rs` (new), `core/skill_discovery.rs` (adds `Deserialize`), `SkillsList.tsx`, `skillPresentation.ts`, `useExploreState.ts`. Known limitation declared: no finalize rollback on replay failure (ticket scoped it out).
- Standards sources: `AGENTS.md` (invariants, ambiguity resolution, "Do not" list — primary standard), `CONTEXT.md` (glossary; this round adds **Edit** / **Fork**), `docs/adr/0001..0003-*.md`, `docs/agents/*.md`.

## Axis 1 — Standards
Report, per file/hunk where relevant: (a) every place the diff violates a documented standard — cite the standard (file + rule); (b) any baseline smell — name it and quote the hunk. Distinguish hard violations from judgement calls: documented-standard breaches can be hard, baseline smells are always judgement calls, and a documented repo standard overrides the baseline. Skip anything tooling enforces (eslint, clippy, rustfmt, tsc, specta binding drift). Under 400 words.

Smell baseline (Fowler, Refactoring ch.3 — *what it is → how to fix*): Mysterious Name → rename; Duplicated Code → extract; Feature Envy → move onto the data; Data Clumps → one type; Primitive Obsession → small type; Repeated Switches → one map/polymorphism; Shotgun Surgery → gather; Divergent Change → split; Speculative Generality → delete; Message Chains → hide; Middle Man → cut; Refused Bequest → composition.

## Axis 2 — Spec
Report: (a) requirements the spec/ticket asked for that are missing or partial; (b) behaviour in the diff that wasn't asked for (scope creep); (c) requirements that look implemented but where the implementation looks wrong. Quote the spec/ticket line for each finding. Under 400 words.

Areas worth particular scrutiny (not exhaustive):
- **Frontmatter writer** (`core/frontmatter_edit.rs` + its tests): is the round-trip actually byte-exact in every corpus case the ticket listed (no frontmatter; neither/one/both keys; odd order; nested indented same-name keys; CRLF; content after `---`; duplicate top-level keys)? Does `write → restore` remove a block the writer created? Does writing the mode the file already has leave bytes untouched? Can a `SKILL.md` whose first line is `---` but has no closing `---` be corrupted? Are the tests property-style over the corpus or a few hand cases?
- **Replay seam** (`installer.rs::finalize_and_propagate_unlocked` → `skill_edits::replay_unlocked`): ordering — is `content_hash` recomputed AFTER the edit is rewritten, and is the row upserted before Propagation reads it? Conflict rule (E6): compared against the **base mode recorded at edit time**, edit wins, row flagged; does `base_value` re-base to the fresh upstream lines on every replay (so a later clear restores *current* upstream)? Does an unresolved conflict persist across later unchanged Updates, and does choosing a mode clear it (re-base) while clearing removes the row? Does replay run for Restore (`central_missing` rebuild) and for Re-point (`unlocatable::repoint_and_update`) — trace the call path, don't assume. What happens when a replay failure occurs after finalize succeeded (declared limitation) — is the resulting state at least consistent (row vs bytes) and reported as `Failed`?
- **Mutation guard**: `set_invocation_override` is a new entry point wrapping its own body in `serialized`; does it call only unlocked seams inside (`propagate_unlocked`) and never another entry point? Is the catalog read inside the guard acceptable?
- **Store / migration**: `skill_edits` table creation on an existing DB (the `hidden_explore_skills` precedent); are edit rows deleted when the skill row is deleted (every deletion path — delete skill, Detach if it deletes, onboarding)? Does `SkillEditKind` reach core as a type, not a string?
- **Typed errors**: `CentralPathMissing` reused for both "central dir gone" and "SKILL.md gone" — acceptable or misleading? Any backend prose surfacing as `OTHER` for an operator-causable condition?
- **DTO / report wire**: `invocation_override: … | null` wire-accurate (not `?`); `invocation_mode` still the effective mode; `SkillRefreshStatusDto::Refreshed` carries `edit_conflict`; is the conflict surfaced in the Refresh/Update warning entries in `useSkillLibrary` with EN + ZH copy, closable like other per-skill warnings?
- **Frontend**: badge as `<button>` inside a clickable card — `stopPropagation`, keyboard access, `aria-label`; modal on the shared `Modal` shell, Save disabled when unchanged (except the declared conflicted-re-base deviation — is that deviation right?), stays open on failure; hook applies the returned DTO in place with no success-path refetch; `useExploreState.ts` change — why was it needed and is it minimal? `skillPresentation.ts` change — does it stay pure and single-implementation?
- **CONTEXT.md**: **Edit** and **Fork** entries match E11; Update/Refresh/Re-point entries cross-link.
- Worktree safety: single commit over `ac8b514`; confirm nothing from v1.2.6 was reverted (`git diff ac8b514..HEAD -- src-tauri/src src/` should show only additions plus the small wiring edits).

## Output
Write your report to `~/Projects/skills-hub-r7b/.scratch/round7/r7-review-<model>.md` (`<model>` = `fable`, `opus` or `astra` — you will be told which) AND return it as your final message. Structure: `## Standards`, `## Spec`, `## Summary` — the summary splits findings into **Blocking** (must fix before v1.2.7 ships: a documented-standard breach, a spec requirement missing/wrong, or a data-safety bug — anything that can corrupt a `SKILL.md`, desync row vs bytes, or lose an operator's edit) vs **Follow-up**. Every finding names file(s) and quotes the hunk or the spec line. Verify every Blocking at HEAD before writing it (`git log -S` if you suspect it is inherited). Be concrete; no generic advice.
