# D1 — A corrupt saved tool selection refuses to sync instead of fanning out to every tool

Status: done — 396374e

Source: BACKLOG #06 (`archive/round10/wave-b-ticketing-evidence.md:57`).

## Change

- `core/settings.rs`: `StoredSelection { Unconfigured, Configured(Vec<String>), Corrupt { detail } }` and one
  reader `read_tool_selection(store, key)` replacing `read_string_list` for `GLOBAL_SELECTED_TOOLS`.
  Blank → `Unconfigured`; parse failure → `Corrupt { detail: <serde message> }`.
  `effective_global_tool_targets`: `Corrupt` → `bail!(SignalError::SettingCorrupt { key, detail })`.
  `load_settings`: `global_selected_tools: None` + new `global_selected_tools_corrupt: bool`.
- `core/errors.rs`: `SignalError::SettingCorrupt { key, detail }`.
- `commands/error.rs`: `CommandError::SettingCorrupt { key, detail }` (wire `SETTING_CORRUPT`).
- `src/bindings/index.ts`: regenerated by `cargo test`.
- `src/commandError.ts`: `SETTING_CORRUPT` in the code map + branch; `src/i18n/resources.ts` EN + ZH
  `errors.settingCorrupt` (names the setting, says "open Configure Tools and save to repair").
- `src/hooks/useSyncOrchestration.ts`: on load, if `global_selected_tools_corrupt`, `notify("warning", …)` once
  with the same repair instruction (widen the reporter `Pick` with `notify`).

## Tests

- `core/tests/settings.rs`: `effective_global_tool_targets` on `"not json"` / `{"a":1}` / `[1,2]` → `Err`
  downcasting to `SettingCorrupt` (replaces the assertion at :257 that pins detection fallback); blank → detection;
  `load_settings` exposes `global_selected_tools_corrupt: true` only for the corrupt rows.
- `src/commandError.test.ts`: `SETTING_CORRUPT` renders the key and detail.
- `src/hooks/useSyncOrchestration.test.ts`: corrupt flag → one warning notification; clean → none.

## Comments

- 2026-09-16 — done in `396374e`. Evidence: `effective_global_tool_targets_refuses_a_corrupt_selection`, `global_selected_tools_parses_json_and_flags_corrupt_rows` (core/tests/settings.rs); `SETTING_CORRUPT` in commandError.test.ts; startup warning in useSyncOrchestration.test.ts.
