# Round-6 review — brief (one child per model; both axes in one brief)

Repo: ~/Projects/skills-hub (main checkout — READ ONLY; do not edit, commit, run npm/cargo mutating commands, or launch the app; `cargo test`/`npm test`/`npm run build` are fine to confirm something). Do not search outside this directory (cloud-synced folders). Do not run `npm run tauri:dev` (it mutates the operator's real skill library). Do not `git add` anything; `.scratch/` is gitignored on purpose. AGENTS.md Workflow step 1 does not apply to a review — start immediately.

## What to review
- Fixed point: `95b7893` (v1.2.5 + toast fix + Re-point policy). HEAD = `6b0ec7d`. Diff: `git diff 95b7893...HEAD`; commits: `git log 95b7893..HEAD --oneline` (9 commits, 4 lanes merged fast-forward after rebase).
- Spec: `.scratch/round6/spec.md` (decisions D1–D10, lanes). Tickets: `.scratch/round6/issues/01..04-*.md`. Implementer claims (verify them) are quoted in `.scratch/round6/implementer-comments.md`. Origin of the round: `.scratch/round5/issues/10-followups.md`.
- Standards sources: `AGENTS.md` (invariants, ambiguity resolution, "Do not" list — primary standard), `CONTEXT.md` (glossary; this round widened **Git acquisition**), `docs/adr/0001..0003-*.md`, `docs/agents/*.md`.

## Axis 1 — Standards
Report, per file/hunk where relevant: (a) every place the diff violates a documented standard — cite the standard (file + rule); (b) any baseline smell — name it and quote the hunk. Distinguish hard violations from judgement calls: documented-standard breaches can be hard, baseline smells are always judgement calls, and a documented repo standard overrides the baseline. Skip anything tooling enforces (eslint, clippy, rustfmt, tsc, specta binding drift). Under 400 words.

Smell baseline (Fowler, Refactoring ch.3 — *what it is → how to fix*): Mysterious Name → rename; Duplicated Code → extract; Feature Envy → move onto the data; Data Clumps → one type; Primitive Obsession → small type; Repeated Switches → one map/polymorphism; Shotgun Surgery → gather; Divergent Change → split; Speculative Generality → delete; Message Chains → hide; Middle Man → cut; Refused Bequest → composition.

## Axis 2 — Spec
Report: (a) requirements the spec/tickets asked for that are missing or partial; (b) behaviour in the diff that wasn't asked for (scope creep); (c) requirements that look implemented but where the implementation looks wrong. Quote the spec/ticket line for each finding. Under 400 words.

Areas worth particular scrutiny (not exhaustive):
- **D1 finalize rename-aside** (`install_finalize.rs`): walk every failure path — move failure, upsert failure, rollback-rename failure, cleanup failure — and confirm bytes+row are consistent or the error names the retained backup. Is the `.skills-hub-old-<uuid>` sibling ever visible to discovery/listing/Unlocatable classification/artifact removal (`EveryGlobalTarget`, `Skill` scope) or the onboarding scan? Can a `central.old` backup left after a double fault break the next Update? Is the `symlink_metadata` collision loop sound? Does `finalize_install` (untouched by design) now differ from `finalize_update` in a way that the ticket's "report, don't widen" note under-states?
- **D3 symlinks are not content** (`content_hash.rs`, `sync_engine.rs`, `onboarding_import.rs`): is the rule applied to every comparison path (`target_has_same_content`, import identity, Propagation same-content, the fingerprint used by onboarding)? A symlink to a *directory* inside the skill — is it skipped consistently by both walkers? Does skipping the symlink's name change hashes of existing skills that have no symlinks (it must not)?
- **D4 branch-with-slash** (`git_acquisition.rs`, `github_download.rs`, **`installer.rs`** — a top-risk shared file per AGENTS.md; the implementer reworked the intent/backfill logic more than the ticket's "minimal, additive" allowed): confirm the parser is unchanged; that the stored `source_subpath` suffix rule cannot pick a wrong branch when the URL path is a legitimate deeper path; that matching-refs failures (404/403/network) never raise the typed GitHub-not-found errors and never turn into a clone retry; that the clone fallback uses the resolved branch; that acquire-first still holds for Re-point (no `upsert_skill` before finalize on any path); that legacy records without `source_subpath` are still backfilled by finalize (the pre-finalize backfill upsert was removed — is anything lost when finalize fails?); and that Add's listing path (`installer.rs` around `resolve_tree_source`) tolerates an API failure.
- **D5–D9 frontend** (`useSkillLibrary.ts`, `App.tsx`, `SkillCard.tsx`, `skillPresentation.ts`, `useStatusReporter.ts`): the stale-action ref pattern vs AGENTS.md's hook conventions; `runSingleRefresh` now returns `true` on completion — is `settleSingleReport(...) ?? true` correct when `action.fail` returns an `ActionExit` (must NOT close the modal), and does any other caller of `runSingleRefresh` care about the new return value? `detailSkill` ownership: does the Explore-detail path (`exploreDetailSkill`) still behave, and is the `activeView === "detail"` fallback to the library view the right UX when a skill vanishes? `repointDoor` vs `sourceKind` — one predicate or two? i18n: `errors.skillGone` present in both `en` and `zh`.
- Worktree safety: 4 lanes merged; confirm nothing from `95b7893` was reverted (`git diff 95b7893..HEAD -- src-tauri/src src/` should show only the intended additions).

## Output
Write your report to `~/Projects/skills-hub/.scratch/round6/r6-review-<model>.md` (`<model>` = `fable`, `opus` or `astra` — you will be told which) AND return it as your final message. Structure: `## Standards`, `## Spec`, `## Summary` — the summary splits findings into **Blocking** (must fix before v1.2.6 ships: a documented-standard breach, a spec requirement missing/wrong, or a data-safety bug) vs **Follow-up**. Every finding names file(s) and quotes the hunk or the spec line. Verify every Blocking at HEAD before writing it (`git log -S` if you suspect it is inherited). Be concrete; no generic advice.
