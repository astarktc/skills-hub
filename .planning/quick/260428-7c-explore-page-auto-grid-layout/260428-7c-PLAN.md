---
phase: 260428-7c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/App.css]
autonomous: true
requirements: [QUICK-260428-7c]

must_haves:
  truths:
    - "Explore page grid columns adjust dynamically based on available width"
    - "Minimum card width is 720px (double the My Skills 360px minimum)"
    - "Both featured skills and search results grids use the auto-fill layout"
  artifacts:
    - path: "src/App.css"
      provides: "Auto-fill grid for explore-grid class"
      contains: "repeat(auto-fill, minmax(720px, 1fr))"
  key_links:
    - from: "src/App.css (.explore-grid)"
      to: "src/components/skills/ExplorePage.tsx"
      via: "className='explore-grid' on both featured and search result grids"
      pattern: "explore-grid"
---

<objective>
Change the Explore page grid from a fixed 2-column layout to a responsive auto-fill grid with a 720px minimum card width (double the My Skills page minimum of 360px).

Purpose: Explore page cards should reflow based on window width, matching the auto-grid behavior already established on the My Skills page but with wider minimum cards for better readability.
Output: Updated `.explore-grid` CSS rule in `src/App.css`.
</objective>

<execution_context>
@/home/alexwsl/skills-hub/.claude/get-shit-done/workflows/execute-plan.md
@/home/alexwsl/skills-hub/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/App.css (line 2040-2045: .explore-grid rule)
@.planning/quick/260428-7c-explore-page-auto-grid-layout/260428-7c-RESEARCH.md

<interfaces>
From src/App.css (lines 2040-2045) -- current rule to modify:
```css
.explore-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 10px;
  margin-bottom: 24px;
}
```

Reference pattern from src/App.css (lines 242-244) -- My Skills auto-grid:

```css
.skills-grid--auto-grid {
  grid-template-columns: repeat(auto-fill, minmax(360px, 1fr));
}
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Change explore-grid to auto-fill with 720px minimum</name>
  <files>src/App.css</files>
  <action>
In `src/App.css` at line 2042, change the `.explore-grid` rule's `grid-template-columns` from:
```css
grid-template-columns: repeat(2, 1fr);
```
to:
```css
grid-template-columns: repeat(auto-fill, minmax(720px, 1fr));
```

Do NOT change `display`, `gap`, or `margin-bottom` -- only the `grid-template-columns` value.

720px = 360px (My Skills auto-grid min) x 2, per user requirement to double the minimum card width.
</action>
<verify>
<automated>grep -n 'repeat(auto-fill, minmax(720px, 1fr))' /home/alexwsl/skills-hub/src/App.css | grep -q 'explore-grid' || (grep -A5 'explore-grid' /home/alexwsl/skills-hub/src/App.css | grep -c 'repeat(auto-fill, minmax(720px, 1fr))')</automated>
</verify>
<done>The `.explore-grid` class uses `repeat(auto-fill, minmax(720px, 1fr))` and `npm run check` passes.</done>
</task>

</tasks>

<verification>
1. `grep 'repeat(auto-fill, minmax(720px, 1fr))' src/App.css` confirms the new rule exists
2. `grep 'repeat(2, 1fr)' src/App.css` does NOT match inside `.explore-grid` (old rule removed)
3. `npm run check` passes (lint + build + rust checks)
</verification>

<success_criteria>

- Explore page grid uses auto-fill with 720px minimum card width
- Both featured skills and search results sections inherit the change (both use `.explore-grid`)
- No other CSS rules affected
- `npm run check` passes
  </success_criteria>

<output>
After completion, create `.planning/quick/260428-7c-explore-page-auto-grid-layout/260428-7c-SUMMARY.md`
</output>
