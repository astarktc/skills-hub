---
phase: quick-260422-jb0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - src/components/projects/AssignmentMatrix.tsx
autonomous: true
---

<objective>
Persist the "Group by repo" checkbox state in both My Skills and Projects pages so it
survives app restarts. Currently both default to `false` on every load.

Purpose: Users who prefer the grouped view have to re-enable it every session. Persisting
this preference via localStorage (matching the existing theme/language pattern) eliminates
that friction.

Output: Both checkboxes restore their last-known state on app load.
</objective>

<execution_context>
@/home/alexwsl/skills-hub/.claude/get-shit-done/workflows/execute-plan.md
@/home/alexwsl/skills-hub/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/App.tsx (lines 46-47 for storage key pattern, lines 106-107 for groupByRepo state,
  lines 294-300 for theme localStorage read pattern, lines 333-344 for theme localStorage
  write pattern, lines 2128-2129 for FilterBar props)
@src/components/projects/AssignmentMatrix.tsx (line 86 for local groupByRepo state)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persist groupByRepo in My Skills (App.tsx)</name>
  <files>src/App.tsx</files>
  <action>
In App.tsx, persist the My Skills `groupByRepo` state to localStorage using the existing
pattern established by theme and language persistence:

1. Add a storage key constant next to the existing ones (~line 47):
   `const groupByRepoStorageKey = "skills-groupByRepo";`

2. Change the `groupByRepo` initializer (~line 107) from `useState(false)` to read from
   localStorage on first render:

   ```
   const [groupByRepo, setGroupByRepo] = useState(() => {
     try {
       return window.localStorage.getItem(groupByRepoStorageKey) === 'true'
     } catch {
       return false
     }
   })
   ```

3. Add a useEffect to write changes to localStorage (place near the theme/language
   persistence effects, around line 310):
   ```
   useEffect(() => {
     if (typeof window === 'undefined') return
     try {
       window.localStorage.setItem(groupByRepoStorageKey, String(groupByRepo))
     } catch {
       // ignore storage failures
     }
   }, [groupByRepo, groupByRepoStorageKey])
   ```

No other files or components change -- the existing prop drilling from App.tsx to
FilterBar and SkillsList already uses the same state variable.
</action>
<verify>
<automated>cd /home/alexwsl/skills-hub && npx tsc --noEmit && npm run lint</automated>
</verify>
<done>My Skills groupByRepo checkbox state is read from localStorage on app load and
written on change. Existing `false` default is preserved when no stored value exists.</done>
</task>

<task type="auto">
  <name>Task 2: Persist groupByRepo in Projects (AssignmentMatrix.tsx)</name>
  <files>src/components/projects/AssignmentMatrix.tsx</files>
  <action>
In AssignmentMatrix.tsx, persist the Projects page `groupByRepo` state using the same
localStorage pattern. This state is local to AssignmentMatrix (not lifted to App.tsx), so
both the read and write happen here:

1. Add a storage key constant near the top of the component function (~line 86):
   `const projectsGroupByRepoKey = "skills-projects-groupByRepo";`

2. Change `useState(false)` to a lazy initializer:

   ```
   const [groupByRepo, setGroupByRepo] = useState(() => {
     try {
       return window.localStorage.getItem(projectsGroupByRepoKey) === 'true'
     } catch {
       return false
     }
   })
   ```

3. Add a useEffect to persist on change:

   ```
   useEffect(() => {
     try {
       window.localStorage.setItem(projectsGroupByRepoKey, String(groupByRepo))
     } catch {
       // ignore storage failures
     }
   }, [groupByRepo])
   ```

   Import `useEffect` if not already imported (check existing imports first -- it is
   likely already imported alongside useState/useMemo).

Use a different localStorage key (`skills-projects-groupByRepo`) than the My Skills page
(`skills-groupByRepo`) so the two pages maintain independent preferences.
</action>
<verify>
<automated>cd /home/alexwsl/skills-hub && npm run check</automated>
</verify>
<done>Projects page groupByRepo checkbox state is read from localStorage on component
mount and written on change. Independent from the My Skills page preference.</done>
</task>

</tasks>

<verification>
1. `npm run check` passes (lint + build + rust checks)
2. Manual: launch app, enable "Group by repo" on My Skills page, switch to another view
   and back -- checkbox stays checked. Restart app -- checkbox is still checked.
3. Manual: same test on the Projects page -- independent persistence.
4. Manual: disable both checkboxes, restart -- both are unchecked.
</verification>

<success_criteria>

- Both groupByRepo checkboxes restore their persisted state on app load
- Two independent localStorage keys: `skills-groupByRepo` and `skills-projects-groupByRepo`
- Follows existing localStorage pattern (try/catch, `typeof window` guard in App.tsx)
- `npm run check` passes with no new warnings
  </success_criteria>

<output>
After completion, create `.planning/quick/260422-jb0-persist-group-by-repo-checkbox-state-in-/260422-jb0-SUMMARY.md`
</output>
