---
phase: 04-frontend-component-tree
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/projects/AssignmentMatrix.tsx
  - src/App.css
autonomous: false
requirements: [UI-03, UI-04, SYNC-01]
must_haves:
  truths:
    - "User can see a checkbox grid of skills (rows) vs configured tools (columns)"
    - "Each assignment cell shows status color: green (synced), yellow (stale), red (error), gray (pending/unchecked)"
    - "Clicking a checkbox toggles assignment with wait-for-backend pending state"
    - "All Tools button on each row bulk-assigns all configured tools"
    - "Toolbar shows project name, path, last sync time, and Sync Project / Sync All / Configure Tools buttons"
    - "Last sync time is derived from assignments and displayed in the toolbar (UI-04)"
    - "Error cells show tooltip on hover and retry on click"
    - "No skills message appears when skill library is empty"
  artifacts:
    - path: "src/components/projects/AssignmentMatrix.tsx"
      provides: "Right panel with toolbar, last sync time, checkbox grid, status indicators"
      exports: ["default"]
    - path: "src/App.css"
      provides: "Matrix grid CSS, cell status colors, toolbar styles, tooltip, last sync time"
      contains: ".matrix-grid"
  key_links:
    - from: "src/components/projects/AssignmentMatrix.tsx"
      to: "useProjectState"
      via: "props from ProjectsPage"
      pattern: "onToggleAssignment|onBulkAssign|onResyncProject"
    - from: "src/App.css"
      to: "src/index.css"
      via: "CSS variable references"
      pattern: "success-soft-bg|warning-soft-bg|danger-soft-bg"
---

<objective>
Build the AssignmentMatrix component (right panel): toolbar with project info and last sync time display (UI-04), sync action buttons, checkbox grid with per-cell status colors, bulk-assign row buttons, error tooltips, and loading skeletons. Wire into ProjectsPage and add remaining CSS. Then visually verify the full Projects tab.

Purpose: Delivers the core assignment interaction (D-04 through D-09, D-21), sync status visualization (SYNC-01), and last sync time display (UI-04). This is the primary feature surface where users manage skill-to-tool mappings.
Output: AssignmentMatrix.tsx, updated ProjectsPage.tsx, matrix CSS in App.css
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frontend-component-tree/04-CONTEXT.md
@.planning/phases/04-frontend-component-tree/04-RESEARCH.md
@.planning/phases/04-frontend-component-tree/04-UI-SPEC.md
@.planning/phases/04-frontend-component-tree/04-01-SUMMARY.md
@.planning/phases/04-frontend-component-tree/04-02-SUMMARY.md

<interfaces>
<!-- Types from prior plans that this plan consumes -->

From src/components/projects/useProjectState.ts (Plan 01):

```typescript
export type ProjectState = {
  projects: ProjectDto[];
  selectedProjectId: string | null;
  tools: ProjectToolDto[];
  assignments: ProjectSkillAssignmentDto[];
  skills: ManagedSkill[];
  toolStatus: ToolStatusDto | null;
  projectsLoading: boolean;
  matrixLoading: boolean;
  pendingCells: Set<string>;
  loadError: string | null;
  showAddModal: boolean;
  showToolConfigModal: boolean;
  showRemoveModal: boolean;
  removeTargetId: string | null;
  // ... action functions
  toggleAssignment: (skillId: string, tool: string) => Promise<void>;
  bulkAssign: (skillId: string) => Promise<void>;
  resyncProject: () => Promise<ResyncSummaryDto>;
  resyncAll: () => Promise<ResyncSummaryDto[]>;
  setShowToolConfigModal: (v: boolean) => void;
  loadToolStatus: () => Promise<void>;
};
```

From src/components/projects/types.ts:

```typescript
export type ProjectDto = {
  id: string;
  path: string;
  name: string;
  tool_count: number;
  assignment_count: number;
  sync_status: string;
};
export type ProjectToolDto = { id: string; project_id: string; tool: string };
export type ProjectSkillAssignmentDto = {
  id: string;
  project_id: string;
  skill_id: string;
  tool: string;
  mode: string;
  status: string;
  last_error?: string | null;
  synced_at?: number | null;
  content_hash?: string | null;
  created_at: number;
};
export type ResyncSummaryDto = {
  project_id: string;
  synced: number;
  failed: number;
  errors: string[];
};
```

From src/components/skills/types.ts:

```typescript
export type ManagedSkill = {
  id: string;
  name: string;
  description?: string | null;
  source_type: string;
  source_ref?: string | null;
  central_path: string;
  created_at: number;
  updated_at: number;
};
```

Matrix cell status mapping (from UI-SPEC + D-08):

- Assignment exists with status "synced" -> green tint (.matrix-cell.synced), checkbox checked
- Assignment exists with status "stale" -> yellow tint (.matrix-cell.stale), checkbox checked
- Assignment exists with status "error" -> red tint (.matrix-cell.error), checkbox checked (shows error)
- Cell in pendingCells Set -> gray tint (.matrix-cell.pending), checkbox indeterminate
- No assignment -> default background, checkbox unchecked

Last sync time derivation (for UI-04):

- Computed from `assignments` array: find the maximum `synced_at` value across all assignments
- If no assignments have `synced_at`, display "Never synced" using `t('projects.lastSyncNever')`
- Otherwise display relative time using `t('projects.lastSyncTime', { time: formattedTime })`
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Create AssignmentMatrix component with toolbar, last sync time, and grid</name>
  <files>src/components/projects/AssignmentMatrix.tsx, src/App.css</files>
  <read_first>
    - src/components/projects/useProjectState.ts (hook API for props interface)
    - src/components/projects/types.ts (ProjectSkillAssignmentDto for status and synced_at fields)
    - src/components/skills/types.ts (ManagedSkill for skill row data)
    - src/App.css (existing classes, append new matrix classes at end after Projects Tab section)
    - src/index.css (CSS variables: --success-soft-bg, --warning-soft-bg, --danger-soft-bg, --bg-element, --accent-primary)
    - .planning/phases/04-frontend-component-tree/04-UI-SPEC.md (spacing, typography, color specs, interaction contracts)
  </read_first>
  <action>
**Create `src/components/projects/AssignmentMatrix.tsx`** per D-04, D-05, D-06, D-07, D-08, D-09, D-21, UI-04:

1. Props type `AssignmentMatrixProps`:
   - `project: ProjectDto | null` (selected project)
   - `tools: ProjectToolDto[]`
   - `assignments: ProjectSkillAssignmentDto[]`
   - `skills: ManagedSkill[]`
   - `pendingCells: Set<string>`
   - `matrixLoading: boolean`
   - `onToggleAssignment: (skillId: string, tool: string) => Promise<void>`
   - `onBulkAssign: (skillId: string) => Promise<void>`
   - `onResyncProject: () => Promise<ResyncSummaryDto>`
   - `onResyncAll: () => Promise<ResyncSummaryDto[]>`
   - `onConfigureTools: () => void`
   - `t: TFunction`

2. Early return: If `!project`, render D-05 placeholder: `<div className="matrix-placeholder">{t('projects.selectProject')}</div>`.

3. **Last sync time derivation (UI-04):** Compute the most recent `synced_at` timestamp from the `assignments` array:

   ```typescript
   const lastSyncAt = useMemo(() => {
     let max = 0;
     for (const a of assignments) {
       if (a.synced_at && a.synced_at > max) max = a.synced_at;
     }
     return max > 0 ? max : null;
   }, [assignments]);
   ```

   Format as relative time using a simple helper function inside the component:

   ```typescript
   function formatRelativeTime(timestampMs: number): string {
     const diffMs = Date.now() - timestampMs;
     const diffSec = Math.floor(diffMs / 1000);
     if (diffSec < 60) return "just now";
     const diffMin = Math.floor(diffSec / 60);
     if (diffMin < 60) return `${diffMin}m ago`;
     const diffHr = Math.floor(diffMin / 60);
     if (diffHr < 24) return `${diffHr}h ago`;
     const diffDay = Math.floor(diffHr / 24);
     return `${diffDay}d ago`;
   }
   ```

4. **Toolbar** (D-04 + UI-04): `.matrix-toolbar` div containing:
   - Left side (`.matrix-toolbar-info`): Project name (`<span className="matrix-toolbar-name">` at 16px/600 heading typography), project path (`<span className="matrix-toolbar-path">` at 12px/400 mono), and **last sync time** (`<span className="matrix-toolbar-sync-time">` at 12px/400 secondary color): display `lastSyncAt ? t('projects.lastSyncTime', { time: formatRelativeTime(lastSyncAt) }) : t('projects.lastSyncNever')`.
   - Right side action buttons (`.matrix-toolbar-actions`):
     - "Configure Tools" button: `Settings` icon (size 14) + `t('projects.addTools')`, `className="btn btn-secondary btn-sm"`, onClick calls `onConfigureTools` (implements TOOL-03).
     - "Sync Project" button: `RefreshCw` icon (size 14) + `t('projects.syncProject')`, `className="btn btn-primary btn-sm"`, onClick calls handleResyncProject.
     - "Sync All" button: `RefreshCw` icon (size 14) + `t('projects.syncAll')`, `className="btn btn-secondary btn-sm"`, onClick calls handleResyncAll.

5. Define handleResyncProject and handleResyncAll as internal callbacks that call the prop functions and show toast results using the returned ResyncSummaryDto:

   ```typescript
   const handleResyncProject = useCallback(async () => {
     try {
       const summary = await onResyncProject();
       if (summary.failed > 0) {
         toast.warning(
           t("projects.resyncPartial", {
             synced: summary.synced,
             failed: summary.failed,
           }),
         );
       } else {
         toast.success(t("projects.resyncSuccess", { synced: summary.synced }));
       }
     } catch (err) {
       toast.error(err instanceof Error ? err.message : String(err));
     }
   }, [onResyncProject, t]);

   const handleResyncAll = useCallback(async () => {
     try {
       const summaries = await onResyncAll();
       const totalSynced = summaries.reduce((sum, s) => sum + s.synced, 0);
       const totalFailed = summaries.reduce((sum, s) => sum + s.failed, 0);
       if (totalFailed > 0) {
         toast.warning(
           t("projects.resyncPartial", {
             synced: totalSynced,
             failed: totalFailed,
           }),
         );
       } else {
         toast.success(t("projects.resyncSuccess", { synced: totalSynced }));
       }
     } catch (err) {
       toast.error(err instanceof Error ? err.message : String(err));
     }
   }, [onResyncAll, t]);
   ```

6. **No skills message**: If `skills.length === 0`, render `<div className="matrix-no-skills">{t('projects.noSkills')}</div>` instead of the grid.

7. **No tools message**: If `tools.length === 0`, render a message encouraging the user to configure tools, with a button that calls `onConfigureTools`.

8. **Matrix grid** (D-08): If `matrixLoading`, render skeleton: 4 rows x 3 columns of `.skeleton-cell` divs with shimmer animation (D-21).

   Otherwise render a scrollable grid:

   a. **Header row** (`.matrix-header-row`): First cell is empty (skill name column). Then one `.matrix-header-cell` per tool in `tools` array, displaying `tool.tool` as the column header (font-size 12px, uppercase, text-align center).

   b. **Skill rows** (`.matrix-row` per skill): For each skill in `skills`:
   - First cell (`.matrix-skill-cell`): skill name (14px/600) with optional description tooltip.
   - One `.matrix-cell` per tool. For each cell, determine state:
     - `cellKey = \`${skill.id}:${tool.tool}\``
     - `isPending = pendingCells.has(cellKey)`
     - `assignment = assignments.find(a => a.skill_id === skill.id && a.tool === tool.tool)`
     - CSS class: `isPending ? 'pending' : assignment ? assignment.status : ''` (status is 'synced' | 'stale' | 'error')
     - Checkbox: `<input type="checkbox" checked={!!assignment && !isPending} disabled={isPending} onChange={() => onToggleAssignment(skill.id, tool.tool)} aria-label={\`${skill.name} - ${tool.tool}\`} />`
     - If `isPending`: Add a small spinner indicator (CSS animation on a pseudo-element, or a `<span className="cell-spinner" />`)
     - If assignment has status `'error'` (D-09): Add `title={t('projects.syncErrorPrefix') + (assignment.last_error ?? '')}` on the cell div for hover tooltip. Add `aria-describedby` pointing to a visually hidden error text span. Make the cell clickable to retry: `onClick={() => onToggleAssignment(skill.id, tool.tool)}` (clicking an error cell retries the sync).
   - Last cell in row: "All Tools" button (`.matrix-all-tools-btn`): `<button className="btn btn-xs matrix-all-tools-btn" onClick={() => onBulkAssign(skill.id)}>{t('projects.allTools')}</button>` per D-07.

9. Use `memo()` on the component. Also consider memoizing individual rows if performance becomes an issue (Claude's discretion from CONTEXT.md allows this).

10. Export: `export default memo(AssignmentMatrix)`

11. Icons needed from lucide-react: `RefreshCw`, `Settings`. Import `{ RefreshCw, Settings } from 'lucide-react'`.

**Add matrix CSS to `src/App.css`** (append after the existing Projects Tab section):

```css
/* === Assignment Matrix === */

.matrix-toolbar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 16px 24px;
  border-bottom: 1px solid var(--border-subtle);
  background: var(--bg-panel);
  gap: 16px;
}

.matrix-toolbar-info {
  display: flex;
  flex-direction: column;
  gap: 2px;
  min-width: 0;
}

.matrix-toolbar-name {
  font-size: 16px;
  font-weight: 600;
  line-height: 1.3;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.matrix-toolbar-path {
  font-size: 12px;
  font-family: var(--font-mono);
  color: var(--text-secondary);
  line-height: 1.4;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.matrix-toolbar-sync-time {
  font-size: 12px;
  color: var(--text-secondary);
  line-height: 1.4;
}

.matrix-toolbar-actions {
  display: flex;
  gap: 8px;
  flex-shrink: 0;
}

.btn-sm {
  padding: 4px 12px;
  font-size: 12px;
  gap: 4px;
}

.btn-xs {
  padding: 2px 8px;
  font-size: 11px;
}

.btn-primary {
  background: var(--accent-primary);
  color: #fff;
  border: none;
  border-radius: var(--radius-md);
  cursor: pointer;
  display: inline-flex;
  align-items: center;
  gap: 4px;
  font-weight: 600;
  transition: opacity 0.15s;
}

.btn-primary:hover {
  opacity: 0.9;
}

.matrix-grid {
  flex: 1;
  overflow: auto;
  padding: 16px 24px;
}

.matrix-grid table {
  border-collapse: collapse;
  width: 100%;
}

.matrix-header-row th {
  font-size: 12px;
  font-weight: 600;
  text-transform: uppercase;
  color: var(--text-secondary);
  text-align: center;
  padding: 8px 4px;
  white-space: nowrap;
}

.matrix-header-row th:first-child {
  text-align: left;
  min-width: 180px;
}

.matrix-row td {
  padding: 4px;
  vertical-align: middle;
}

.matrix-skill-cell {
  font-size: 14px;
  font-weight: 600;
  line-height: 1.4;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  max-width: 200px;
  padding-right: 8px;
}

.matrix-cell {
  text-align: center;
  padding: 8px;
  border-radius: var(--radius-sm);
  transition: background-color 0.15s;
  position: relative;
  min-width: 48px;
}

.matrix-cell.synced {
  background: var(--success-soft-bg);
}

.matrix-cell.stale {
  background: var(--warning-soft-bg);
}

.matrix-cell.error {
  background: var(--danger-soft-bg);
  cursor: pointer;
}

.matrix-cell.pending {
  background: var(--bg-element);
  opacity: 0.7;
}

.matrix-cell input[type="checkbox"] {
  width: 18px;
  height: 18px;
  accent-color: var(--accent-primary);
  cursor: pointer;
}

.matrix-cell.pending input[type="checkbox"] {
  cursor: wait;
}

/* Error tooltip via CSS (D-09) */
.matrix-cell.error[title] {
  cursor: pointer;
}

.matrix-cell.error::after {
  content: attr(title);
  position: absolute;
  bottom: calc(100% + 4px);
  left: 50%;
  transform: translateX(-50%);
  background: var(--bg-tooltip, #1f2937);
  color: var(--text-tooltip, #fff);
  font-size: 11px;
  padding: 4px 8px;
  border-radius: var(--radius-sm);
  white-space: nowrap;
  pointer-events: none;
  opacity: 0;
  transition: opacity 0.15s;
  z-index: 10;
}

.matrix-cell.error:hover::after {
  opacity: 1;
}

.matrix-all-tools-btn {
  white-space: nowrap;
}

.matrix-no-skills {
  display: flex;
  align-items: center;
  justify-content: center;
  height: 200px;
  font-size: 13px;
  color: var(--text-secondary);
  padding: 24px;
  text-align: center;
}

/* Cell spinner for pending state */
.cell-spinner {
  display: inline-block;
  width: 14px;
  height: 14px;
  border: 2px solid var(--border-subtle);
  border-top-color: var(--accent-primary);
  border-radius: 50%;
  animation: spin 0.6s linear infinite;
}

@keyframes spin {
  to {
    transform: rotate(360deg);
  }
}

/* Matrix skeleton loading (D-21) */
.matrix-skeleton {
  display: grid;
  grid-template-columns: 180px repeat(3, 1fr);
  gap: 8px;
  padding: 16px 24px;
}

.skeleton-cell {
  height: 36px;
  border-radius: var(--radius-sm);
  background: linear-gradient(
    90deg,
    var(--bg-element) 0%,
    var(--bg-element-hover) 50%,
    var(--bg-element) 100%
  );
  background-size: 200% 100%;
  animation: shimmer 1.5s infinite linear;
}

.matrix-content {
  display: flex;
  flex-direction: column;
  flex: 1;
  overflow: hidden;
}
```

  </action>
  <verify>
    <automated>cd /home/alexwsl/skills-hub && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>AssignmentMatrix renders toolbar with project info, last sync time (UI-04), and sync action buttons. Checkbox grid with per-cell status colors (green/yellow/red/gray), All Tools bulk-assign button per row, error tooltips on hover, and skeleton loading state. resync handlers use ResyncSummaryDto return values for toast display. All CSS classes are in App.css using existing CSS variables.</done>
</task>

<task type="auto">
  <name>Task 2: Wire AssignmentMatrix into ProjectsPage and finalize</name>
  <files>src/components/projects/ProjectsPage.tsx</files>
  <read_first>
    - src/components/projects/ProjectsPage.tsx (current state from Plan 02 -- has ProjectList and modals wired, matrix placeholder)
    - src/components/projects/AssignmentMatrix.tsx (just created -- read to verify exact props interface)
    - src/components/projects/useProjectState.ts (hook API for resync return types)
  </read_first>
  <action>
**Update `src/components/projects/ProjectsPage.tsx`** to wire AssignmentMatrix:

1. Add import: `import AssignmentMatrix from './AssignmentMatrix'`

2. Add resync handlers that pass through the ResyncSummaryDto return values. The hook's `resyncProject` returns `Promise<ResyncSummaryDto>` and `resyncAll` returns `Promise<ResyncSummaryDto[]>` (standardized in Plan 01):

   ```typescript
   const handleResyncProject = useCallback(async () => {
     return await state.resyncProject();
   }, [state]);

   const handleResyncAll = useCallback(async () => {
     return await state.resyncAll();
   }, [state]);
   ```

   Note: Toast display for resync results is handled inside AssignmentMatrix (Task 1) using the returned summary DTOs. The ProjectsPage handlers simply pass through the hook's return values.

3. Add toggle and bulk-assign handlers that show error toasts:

   ```typescript
   const handleToggleAssignment = useCallback(
     async (skillId: string, tool: string) => {
       try {
         await state.toggleAssignment(skillId, tool);
       } catch (err) {
         toast.error(err instanceof Error ? err.message : String(err));
       }
     },
     [state],
   );

   const handleBulkAssign = useCallback(
     async (skillId: string) => {
       try {
         await state.bulkAssign(skillId);
       } catch (err) {
         toast.error(err instanceof Error ? err.message : String(err));
       }
     },
     [state],
   );
   ```

4. Add handler for Configure Tools from matrix toolbar (reuses existing tool config flow):

   ```typescript
   const handleConfigureToolsFromToolbar = useCallback(async () => {
     await state.loadToolStatus();
     state.setShowToolConfigModal(true);
   }, [state]);
   ```

5. Replace the matrix placeholder section in the JSX. Find the `<section className="matrix-panel">` block and replace its contents:

   ```jsx
   <section className="matrix-panel">
     {!state.selectedProjectId ? (
       <div className="matrix-placeholder">{t("projects.selectProject")}</div>
     ) : (
       <div className="matrix-content">
         <AssignmentMatrix
           project={
             state.projects.find((p) => p.id === state.selectedProjectId) ??
             null
           }
           tools={state.tools}
           assignments={state.assignments}
           skills={state.skills}
           pendingCells={state.pendingCells}
           matrixLoading={state.matrixLoading}
           onToggleAssignment={handleToggleAssignment}
           onBulkAssign={handleBulkAssign}
           onResyncProject={handleResyncProject}
           onResyncAll={handleResyncAll}
           onConfigureTools={handleConfigureToolsFromToolbar}
           t={t}
         />
       </div>
     )}
   </section>
   ```

6. Ensure all imports are correct and no unused variables remain. The component should now have: useProjectState, ProjectList, AddProjectModal, ToolConfigModal, RemoveProjectModal, AssignmentMatrix, toast, useTranslation, memo, useCallback.
   </action>
   <verify>
   <automated>cd /home/alexwsl/skills-hub && npm run check 2>&1 | tail -30</automated>
   </verify>
   <done>AssignmentMatrix is wired into ProjectsPage with all props connected. Resync handlers pass through ResyncSummaryDto return values (consistent with Plan 01 hook contract). Toggle, bulk-assign, resync, and tool-config flows work end-to-end through the hook. npm run check passes.</done>
   </task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete Projects tab</name>
  <files></files>
  <action>
Visual verification of the complete Projects tab UI. All automated components have been built and wired in Tasks 1 and 2. This checkpoint verifies the integrated result in the Tauri dev window.
  </action>
  <verify>
    <automated>cd /home/alexwsl/skills-hub && npm run check 2>&1 | tail -10</automated>
  </verify>
  <done>
Human has verified all 15 visual/functional checks and typed "approved", or has described specific issues for resolution.

Verification steps for the human:

1. Run `npm run tauri:dev` to start the app
2. Click the "Projects" tab in the navigation bar -- should show full-width empty state (D-03)
3. Verify empty state shows icon, message, and "Add Project" button (full-width, not in left panel)
4. Click "Add Project" -- modal should open with folder picker and manual path input
5. Verify gitignore checkboxes appear (unchecked by default) in the registration modal (D-13)
6. Register a project directory (e.g., the skills-hub directory itself)
7. After registration, tool config modal should auto-open with installed tools pre-checked
8. Confirm tool selection -- matrix columns should appear
9. Verify project appears in left panel with name, path, assignment count, status dot
10. Verify toolbar shows project name, path, and last sync time ("Never synced" initially) (UI-04)
11. In the matrix, check a skill-tool checkbox -- should show pending state then green (synced)
12. Verify last sync time updates in toolbar after a successful sync (UI-04)
13. Click "All Tools" on a skill row -- all cells should go pending then update
14. Click "Sync Project" -- should sync and show toast with synced/failed counts
15. Click "Sync All" -- should sync all projects and show aggregate toast
16. Click "Configure Tools" in toolbar -- tool config modal should reopen
17. Try registering a duplicate project path -- should show inline error
18. Remove a project via the trash icon -- confirmation modal should appear
19. Verify dark mode works (toggle theme in Settings, check all status colors)
    </done>
    </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                          |
| ------------------------------------ | ---------------------------------------------------- |
| Matrix checkbox click -> backend IPC | Each toggle sends assign/unassign command to backend |
| Error message display                | Backend error strings displayed in tooltip           |

## STRIDE Threat Register

| Threat ID | Category               | Component                                      | Disposition | Mitigation Plan                                                                                                                                     |
| --------- | ---------------------- | ---------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-04-07   | Tampering              | Matrix toggle with forged skillId/tool         | accept      | Frontend sends IDs from backend-provided data. Backend validates all IDs against database. No user-editable ID fields.                              |
| T-04-08   | Information Disclosure | Error message in tooltip                       | accept      | Error messages from backend are filesystem operation errors (path not found, permission denied). No sensitive data. Single-user desktop app.        |
| T-04-09   | Denial of Service      | Rapid checkbox clicking causing many IPC calls | mitigate    | pendingCells Set prevents duplicate in-flight requests for the same cell. Wait-for-backend pattern (D-06) naturally serializes per-cell operations. |

</threat_model>

<verification>
1. `npm run check` passes
2. Matrix renders checkbox grid with tools as columns and skills as rows
3. Cell status colors match: green (synced), yellow (stale), red (error), gray (pending)
4. Checkbox toggle shows pending state then updates (D-06)
5. "All Tools" button bulk-assigns all configured tools for a skill (D-07)
6. Error cells show tooltip on hover (D-09)
7. Toolbar shows project name, path, last sync time, sync buttons (D-04, UI-04)
8. Last sync time is derived from assignment synced_at timestamps and displayed as relative time (UI-04)
9. Skeleton loading appears during matrix data fetch (D-21)
10. Visual verification in Tauri dev window confirms full flow
</verification>

<success_criteria>

- Assignment matrix renders with dynamic tool columns and skill rows
- Per-cell status colors (green/yellow/red/gray) are visually distinct in both light and dark modes
- Checkbox toggle follows wait-for-backend pattern with pending state
- Bulk assign works for all tools in a skill row
- Sync Project and Sync All buttons trigger resync with toast showing synced/failed counts from ResyncSummaryDto
- Last sync time appears in toolbar, derived from assignment synced_at, updates after sync (UI-04)
- Configure Tools button opens tool config modal from toolbar
- Error tooltip shows on hover over red cells
- Skeleton loading shows during data fetch
- Full Projects tab flow verified visually in Tauri dev window
- `npm run check` exits 0
  </success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-component-tree/04-03-SUMMARY.md`
</output>
