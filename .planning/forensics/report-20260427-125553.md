# Forensic Report

**Generated:** 2026-04-27T12:55:53-05:00
**Problem:** Fresh final audit current codebase for anything missed between v1.1.3 and re-created v1.1.4 after improper worktree merges; compare current main against intended v1.1.4/release history and identify missing work/regressions.

---

## Evidence Summary

### Git Activity

- **Last commit:** 2026-04-27 12:35:22 -0500 — `154e988 docs: add worktree safety guidance to CLAUDE.md`
- **Commits (last 30):** 30
- **Time span (last 30):** 2026-04-22 12:32:19 -0500 → 2026-04-27 12:35:22 -0500
- **Release range:** `v1.1.3` = `1bc9358`; `v1.1.4` / current `main` = `154e988`
- **Recovery commits after recreated release merge:**
  - `5e1f42e fix: restore 18 features lost during parallel worktree merges`
  - `154e988 docs: add worktree safety guidance to CLAUDE.md`
- **Uncommitted changes before report write:** only untracked planning/debug files:
  - `.planning/debug/bulk-import-skill-lock-failure.md`
  - `.planning/debug/macos-damaged-app-signing.md`
  - `plan-mode-archive/2026-04-27-1223-compiled-waddling-lovelace.md`
- **Active worktrees:** 1 — main worktree only.
- **Most-edited recent files:** `src/App.tsx` (8), `src/i18n/resources.ts` (6), `.planning/STATE.md` (6), `src/components/projects/AssignmentMatrix.tsx` (5), `src-tauri/src/core/installer.rs` (5).

### Verification

- `npm run check` passed on current `v1.1.4` / `main`.
  - ESLint: 0 errors, 3 warnings in copied GSD script files under `.claude/.codex/.opencode`.
  - Build: passed with Vite chunk-size/dynamic-import warnings.
  - Rust fmt check: passed.
  - Clippy with `-D warnings`: passed.
  - Rust tests: 164 unit tests + 16 integration tests passed.
- `git diff --check v1.1.3..v1.1.4` found no whitespace errors.

### Planning State

- **Current milestone:** `v1.0 Per-Project Skill Distribution -- SHIPPED 2026-04-09` in `.planning/STATE.md`.
- **Current phase:** none active; STATE says milestone complete and next step is `/gsd-new-milestone`.
- **Blockers:** none in `.planning/STATE.md`.
- **Config:** `.planning/config.json` has `use_worktrees: true`, `parallelization: true`, and quality workflow toggles enabled.
- **Session report:** `.planning/reports/SESSION_REPORT.md` does not exist.

### Artifact Completeness

| Phase                      | PLAN | CONTEXT | RESEARCH | SUMMARY | VERIFICATION |
| -------------------------- | ---- | ------- | -------- | ------- | ------------ |
| 01 Data Foundation         | yes  | yes     | yes      | yes     | yes          |
| 02 Sync Logic              | yes  | yes     | yes      | yes     | yes          |
| 03 IPC Commands            | yes  | yes     | yes      | yes     | yes          |
| 04 Frontend Component Tree | yes  | yes     | yes      | yes     | yes          |
| 05 Edge Cases and Polish   | yes  | yes     | no       | yes     | yes          |
| 06 Gap Closure             | yes  | yes     | no       | yes     | yes          |

Notes: phases 05 and 06 have no `*-RESEARCH.md`, but they do have plans, summaries, reviews, and verification artifacts. This looks like workflow variance, not direct evidence of lost v1.1.4 code.

### Intended v1.1.3 → v1.1.4 Work Checked

Implementation commits in the release range included:

- Copaw tool adapter (`7532f21`)
- Git install/frontmatter fixes (`6d72c08`, `b6fc41f`)
- Hermes adapter, `project_relative_skills_dir()`, `overwriteIfSameContent`, window sizing (`071a1cf`)
- Marketplace path traversal fix (`88f2aa9`)
- Project-relative cleanup fix (`cd9d962`)
- Source hash caching and assignment map optimization (`68d6101`, `6ec5564`)
- Copy-mode project re-sync on update (`74add86`, `457c052`)
- Refresh decoupled from auto-sync (`23c8def`, `e7db399`)
- SkillCard link/unlink toggle (`be0b104`)
- Group-by-repo localStorage persistence (`b8ece0e`, `66a521e`)
- Projects toolbar alignment (`f65bf45`)
- Explore page skill detail preview and install-from-preview flow (`be626ba`, `1a5b779`)
- Version bump to `1.1.4` (`3c11832`)

Direct symbol checks found current code contains the major restored implementation surfaces:

- `ToolId::Copaw`, `ToolId::HermesAgent`, and `project_relative_skills_dir()` in `src-tauri/src/core/tool_adapters/mod.rs`.
- `overwriteIfSameContent` command parameter in `src-tauri/src/commands/mod.rs` and call sites in `src/App.tsx`.
- `clone_or_pull_sparse()` in `src-tauri/src/core/git_fetcher.rs` and sparse install usage in `src-tauri/src/core/installer.rs`.
- `normalize_github_skill_subpath()`, `ensure_installable_skill_dir()`, and `collect_skill_dirs()` in `src-tauri/src/core/installer.rs`.
- `parse_marketplace_json()` canonical path validation in `src-tauri/src/core/installer.rs`.
- `blockLines` frontmatter rendering in `src/components/skills/SkillDetailView.tsx`.
- `update_skill_content_hash()` in `src-tauri/src/core/skill_store.rs` and `skill_cache` in `src-tauri/src/core/project_sync.rs`.
- Copy-mode update re-sync logic and `update_resyncs_project_copy_assignments` test in `src-tauri/src/core/installer.rs` / `src-tauri/src/core/tests/installer.rs`.
- `handleSyncSkillToAllTools` and link/unlink UI in `src/App.tsx` / `src/components/skills/SkillCard.tsx`.
- My Skills and Projects `groupByRepo` localStorage persistence in `src/App.tsx` and `src/components/projects/AssignmentMatrix.tsx`.
- Explore preview command and UI flow in `src-tauri/src/commands/mod.rs`, `src-tauri/src/core/installer.rs`, `src-tauri/src/lib.rs`, `src/App.tsx`, `src/components/skills/ExplorePage.tsx`, and `src/components/skills/SkillDetailView.tsx`.

### Independent Audit Cross-Check

A parallel read-only audit independently verified the 18 source/test features named in `5e1f42e` and reported no source-level omissions or regressions. That result agrees with the direct symbol checks above. The one remaining anomaly below is docs/catalog release-support work, not application source code.

## Anomalies Detected

### Missing docs/catalog work from the earlier release history — Confidence: HIGH

**Evidence:** The `v1.1.3..v1.1.4` history includes docs/catalog commits:

- `25deacf docs: add Copaw to supported tools list`
- `c825f49 docs: add Copaw to supported tools list in README.zh.md`
- `0cb20e5 docs: add v0.4.3 release notes for Copaw support`
- `0878b2e docs: add contributor and PR link to v0.4.3 release notes`
- `9ba0a6f docs: add v0.4.3 bugfix notes`
- `3757f63 chore: update featured-skills.json`

But final `v1.1.4` has no diff from `v1.1.3` for `README.md`, `docs/README.zh.md`, `docs/releases/v0.4.3/`, or `featured-skills.json`. Comparing current `v1.1.4` to `3757f63` shows missing differences:

- `README.md`: +1 Copaw supported-tool row
- `docs/README.zh.md`: +1 Copaw supported-tool row
- `docs/releases/v0.4.3/add-copaw-support.md`: new 26-line release note
- `docs/releases/v0.4.3/bugfix-github-install-and-frontmatter.md`: new 206-line release note
- `featured-skills.json`: 630 insertions / 630 deletions

Sample missing line: `| copaw | Copaw | .copaw/skill_pool | .copaw |` in both README tables.

**Interpretation:** The code-level Copaw adapter was restored, but associated documentation, release notes, and catalog refresh from the pre-damage history were not restored into the final `v1.1.4` tag.

### Prior worktree merge corruption confirmed by recovery commits — Confidence: HIGH

**Evidence:** `5e1f42e` explicitly states: “Commit `68d6101` silently reverted changes from earlier worktrees. Partial recovery was done in `e90d554` but many features remained missing.” The commit changed 19 source/test files with 1,337 insertions and restored 18 named features. `e7db399` separately restored `handleRefresh` batch update logic lost during a parallel worktree merge.

**Interpretation:** The repository did experience broad silent reversion from stale worktree merging. Current source appears substantially recovered, but this history explains why doc/catalog work outside the recovery checklist could still be missing.

### Stale roadmap progress table — Confidence: MEDIUM

**Evidence:** `.planning/STATE.md` marks the milestone complete with 6/6 phases and 12/12 plans. `.planning/ROADMAP.md` has checked plan entries for all phases but its Progress table still says every phase is “0/N Not started”.

**Interpretation:** Planning metadata is internally inconsistent. It does not indicate source regression, but it weakens roadmap-based auditing because the progress table cannot be trusted without cross-checking plan entries and STATE.

### Missing session report — Confidence: LOW

**Evidence:** `.planning/reports/SESSION_REPORT.md` does not exist, while the forensics workflow expects it if available.

**Interpretation:** No direct recovery issue, but the absence removes one evidence source for final-session outcomes.

### No abandoned/crashed worktree signal — Confidence: HIGH negative finding

**Evidence:** `git worktree list` shows only the main worktree. `git status --short` showed only untracked planning/debug files before this report. STATE shows no active execution.

**Interpretation:** There is no current evidence of an orphaned worktree, interrupted merge, or active execution left half-finished.

### No current test/build regression signal — Confidence: HIGH negative finding

**Evidence:** `npm run check` passed, including TypeScript build, Vite build, Rust fmt, clippy with warnings denied, 164 Rust unit tests, and 16 integration tests.

**Interpretation:** The current codebase is structurally healthy. This does not prove every UX path works, but it reduces likelihood of compile/test-level omissions in recovered source code.

## Root Cause Hypothesis

The main implementation work between `v1.1.3` and `v1.1.4` appears to have been restored by `5e1f42e`, and current checks pass. The remaining likely miss is non-source release-support work: Copaw README rows, v0.4.3 release-note files, and the `featured-skills.json` refresh were present in the earlier history but absent from the final recreated `v1.1.4` tag. This likely happened because the recovery checklist focused on source/test features lost by stale worktree merges and did not include docs/catalog commits.

## Recommended Actions

1. Restore the missing docs/catalog work if it is still desired for `v1.1.4`:
   - `README.md` Copaw row
   - `docs/README.zh.md` Copaw row
   - `docs/releases/v0.4.3/add-copaw-support.md`
   - `docs/releases/v0.4.3/bugfix-github-install-and-frontmatter.md`
   - `featured-skills.json` update from `3757f63` or a fresh catalog generation if preferred
2. Before retagging or shipping, run `npm run check` again after the docs/catalog restoration.
3. If the final release tag should represent only code + current app metadata, explicitly decide to omit those docs/catalog changes and record that decision, because they are the only high-confidence missing work found.
4. Treat `.planning/ROADMAP.md` progress table as stale until repaired; use `.planning/STATE.md` and per-phase artifacts as the authoritative planning evidence.

---

_Report generated by `/gsd-forensics`. Paths are project-relative for portability._
